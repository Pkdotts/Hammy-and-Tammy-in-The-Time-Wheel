; Godot Engine scene file representing a simple 2D platform.

[gd_scene load_steps=2 format=2]

# External resource: the texture used for the platform's sprite.
[ext_resource path="res://plataforma.png" type="Texture" id=1]

# Root node for this scene.  We use Node2D as the base for 2D scenes.
[node name="Plataforma" type="Node2D"]

# A visual representation of the platform.  This Sprite uses the
# external texture defined above.  Adjust its position if needed.
[node name="Sprite" type="Sprite" parent="."]
texture = ExtResource( 1 )

# The StaticBody2D makes the platform solid so that physics bodies
# (such as a player character) can stand on it.  It contains a
# CollisionShape2D child defining the collision boundaries.
[node name="StaticBody2D" type="StaticBody2D" parent="."]
position = Vector2( 0, 0 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="StaticBody2D"]
position = Vector2( 0, 0 )
shape = RectangleShape2D {
    extents = Vector2( 128, 32 )
}